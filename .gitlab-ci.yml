validate:
  image: gramener/builderrors
  script: builderrors

deploy:
  stage: deploy
  script: deploy
  variables:
    SERVER: gramener.com
    URL: researchintegrity
    VERSION: static
    SETUP: npm install --omit=dev
    CORS: enabled
  environment:
    name: production
    url: https://gramener.com/researchintegrity/
